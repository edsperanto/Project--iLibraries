var iDom = (function() {

  var structure = {

  };

  function _find(element) {

  }

  to.setting.newPropertyOfList('sibling').to(document).in(structure);

  return {
    find: _find
  };
});

page = iDom();